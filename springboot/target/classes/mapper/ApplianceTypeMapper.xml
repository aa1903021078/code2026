<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ApplianceTypeMapper">

    <!-- 新增 -->
    <insert id="insert" parameterType="com.example.entity.ApplianceType" useGeneratedKeys="true" keyProperty="id">
        insert into appliance_type (
            name, icon, unit, price_min, price_max, weight_estimate,
            points_per_kg, carbon_factor, description, sort_order, status
        ) values (
                     #{name}, #{icon}, #{unit}, #{priceMin}, #{priceMax}, #{weightEstimate},
                     #{pointsPerKg}, #{carbonFactor}, #{description}, #{sortOrder}, #{status}
                 )
    </insert>

    <!-- 删除 -->
    <delete id="deleteById">
        delete from appliance_type where id = #{id}
    </delete>

    <!-- 修改 -->
    <update id="updateById" parameterType="com.example.entity.ApplianceType">
        update appliance_type
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="unit != null">unit = #{unit},</if>
            <if test="priceMin != null">price_min = #{priceMin},</if>
            <if test="priceMax != null">price_max = #{priceMax},</if>
            <if test="weightEstimate != null">weight_estimate = #{weightEstimate},</if>
            <if test="pointsPerKg != null">points_per_kg = #{pointsPerKg},</if>
            <if test="carbonFactor != null">carbon_factor = #{carbonFactor},</if>
            <if test="description != null">description = #{description},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        where id = #{id}
    </update>

    <!-- 根据ID查询 -->
    <select id="selectById" resultType="com.example.entity.ApplianceType">
        select * from appliance_type where id = #{id}
    </select>

    <!-- 查询所有 -->
    <select id="selectAll" resultType="com.example.entity.ApplianceType">
        select * from appliance_type
        <where>
            <if test="name != null">and name like concat('%', #{name}, '%')</if>
            <if test="status != null">and status = #{status}</if>
        </where>
        order by sort_order asc, create_time desc
    </select>

    <!-- 查询启用的品类列表 -->
    <select id="selectEnabled" resultType="com.example.entity.ApplianceType">
        select * from appliance_type where status = 1 order by sort_order asc
    </select>

    <!-- 根据名称查询 -->
    <select id="selectByName" resultType="com.example.entity.ApplianceType">
        select * from appliance_type where name = #{name} limit 1
    </select>


</mapper>